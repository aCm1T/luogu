# P3992 [BJOI2017] 开车

## 题意

$n$ 辆车与 $n$ 个加油站在数轴上一一匹配，代价为 $|x-y|$，求最小总代价；支持 $q$ 次修改某辆车的位置。

## 做法

排序后一一配对最优。离散化后把车看作 $+1$、加油站看作 $-1$，前缀和 $f_i$ 表示走到坐标点 $i$ 时尚未匹配的差值，相邻点之间线段贡献为 $|f_i|\cdot \Delta x$。

移动一辆车等价于一段区间加减 $1$。用分块维护：块内按 $f$ 排序并打懒标记，整块更新时二分正负分界，暴力块重构。

复杂度 $O((n+q)\sqrt V\log V)$（$V$ 为离散点数）。
