# P17094 [ICPC 2017 Qingdao R] Collecting Cents 题解

## 题意简述

初始有 $n$ 美分，银行利率恒为 $1/d$。把 $c$ 美分存定期 $t$ 年（$c,t$ 均为正整数），到期取回

$$
c + \left\lfloor \frac{ct}{d} \right\rfloor
$$

多笔存款**独立计息**。求 $y$ 年后能拥有的最大美分数。

**约束**：$n < 10^8$，$d < 7$，$y < 100$，且 $n(1+1/d)^y < 2^{64}$；测试点不超过 $120$ 组。

---

## 难点在哪

若没有下取整，任意拆分、合并、长短搭配最终结果都一样，答案就是 $n$ 连续滚一年期 $y$ 次。  
有了 $\lfloor\cdot\rfloor$ 之后：

- 同期限两笔存款合并计息**不亏**（可能更赚）；
- 一笔长存拆成两段短存，有时利息更多、有时更少；
- 「锁多久、锁多少」会改变中间各年的**可用现金**，从而影响后续决策。

因此必须在「利息」与「流动性」之间做搜索，而不能只贪心最长存期。

---

## 核心观察

### 1. 同期限存款至多一笔

对固定期限 $t$：

$$
\left\lfloor\frac{at}{d}\right\rfloor + \left\lfloor\frac{bt}{d}\right\rfloor \le \left\lfloor\frac{(a+b)t}{d}\right\rfloor
$$

同期限多笔应先合并再存，故每年对每个期限 $t$ 只需考虑**至多一笔**存款。

### 2. 长期存款本金只需考虑 $1\sim d$

若一笔期限为 $t$ 的存款本金 $c > d$，可拆出 $d$ 美分改走一年期滚动。  
由于 $\lfloor ct/d\rfloor = \lfloor (c-d)t/d\rfloor + t$，最终本息在「全部锁死」与「$d$ 美分逐年滚」两种做法下可以对齐，但后者每年多出可动用的利息，策略空间严格不差。因此最优解中，任意一笔**长期**存款的本金都可以压到 $\le d$。

### 3. 期限超过 $d$ 的无用

$d < 7$，可选期限本身就很小。更一般地，期限 $> d$ 的存款可以在第 $d$ 年切开再存，不会比「直接锁更久」更差；实现里只需枚举期限 $2\sim d$（一年期单独处理）。

### 4. 可无损拆分的长期存款丢弃

对期限 $t$、本金 $c$，若存在切点 $1\le k < t$，使得

$$
c' = c + \left\lfloor\frac{ck}{d}\right\rfloor,\qquad
c' + \left\lfloor\frac{c'(t-k)}{d}\right\rfloor \ge c + \left\lfloor\frac{ct}{d}\right\rfloor
$$

则「先存 $k$ 年再续存」利息不低于一次存满 $t$ 年，且中间多一次取款机会。这类 $(c,t)$ 不必作为原子操作枚举。

对各 $d$ 筛完后，真正有用的长期存款很少，例如：

| $d$ | 有用的 $(t,c)$ |
|-----|----------------|
| $2$ | $(2,1)$ |
| $3$ | $(2,2),\ (3,1)$ |
| $4$ | $(2,2),\ (2,3),\ (4,1)$ |
| $5$ | $(2,3),\ (2,4),\ (3,2),\ (5,1)$ |
| $6$ | $(2,3),\ (2,4),\ (2,5),\ (3,2),\ (3,4),\ (6,1)$ |

### 5. 剩余现金一律走一年期

每年先决定若干笔互不冲突的长期存款（每期限至多一笔），剩下的现金全部按一年期存：

$$
x \;\mapsto\; x + \left\lfloor\frac{x}{d}\right\rfloor
$$

不参与长期存款的剩余现金应全部存一年期；与闲置相比，一年期会额外获得利息，绝不会更差。

---

## 状态设计

把未来的回款日程压成向量。令

$$
\texttt{money}[0]=\text{当前现金},\quad
\texttt{money}[i]=\text{$i$ 年后到期的本息}\quad(1\le i\le d)
$$

一年结束时：

1. 从现金里扣掉各笔长期存款本金；
2. 剩余现金做一年期，连同原 $\texttt{money}[1]$ 变成下一年的现金；
3. $\texttt{money}[i]\leftarrow\texttt{money}[i+1]$（日程前移）；
4. 期限为 $t$ 的存款，把 $c+\lfloor ct/d\rfloor$ 加到 $\texttt{money}[t-1]$。

这样状态空间与「手里有多少笔存单」无关，只与回款时间表有关。

---

## 支配剪枝

状态 $A$ **支配** $B$，当且仅当对每个前缀 $0\le k\le d$：

$$
\sum_{i=0}^{k} A.\texttt{money}[i] \ge \sum_{i=0}^{k} B.\texttt{money}[i]
$$

含义：$A$ 在任意未来时刻，累计已解冻的钱都不少于 $B$，因此 $B$ 能做的存款策略 $A$ 都能复现。保留帕累托前沿即可。

每年转移后：去重 → 按支配关系删掉被压制的状态。

```mermaid
flowchart LR
    S0[当年状态集] --> C[枚举长期存款组合]
    C --> Y[剩余现金一年期]
    Y --> N[生成下一年状态]
    N --> D[去重 + 支配剪枝]
    D --> S1[新状态集]
```

---

## 算法流程

对每组 $(n,d,y)$：

1. 预处理有用的长期存款 $(t,c)$，以及「剩余年数 $=\mathrm{rem}$」时所有合法组合（对每个期限选「不存」或某个有用本金；期限 $> \mathrm{rem}$ 的不能选）。
2. 初始状态：$\texttt{money}[0]=n$，其余为 $0$。
3. 对 $\mathrm{year}=0..y-1$：用当前全部状态 × 当前剩余年数下的存款组合做转移，剪枝得到新年状态。
4. 答案为最终所有状态中 $\texttt{money}[0]$ 的最大值。

剩余年数为 $r$ 时，只需看 `choices[min(d,r)]`：更长的存单反正到期晚于终点，不会比截断策略更好。

---

## 复杂度

| 项目 | 量级 |
|------|------|
| $d$ | $< 7$ |
| 有用存款种类 | $O(d^2)$ 且实际个位数 |
| 每年存款组合数 | 对各期限独立选取，总数很小 |
| 状态数（剪枝后） | 实践中很少，远小于朴素枚举 |
| 年份 | $< 100$ |
| 测试点 | $\le 120$ |

总体足以轻松通过；瓶颈不在 asymptotics，而在「组合爆炸 vs 支配剪枝」是否写对。

---

## 实现要点

1. 金额用 `unsigned long long`：题目保证最终结果 $< 2^{64}$。
2. 同一期限多笔在枚举层就禁止出现（DFS 对每个 `term` 只选一次）。
3. 支配判断用**前缀和**，不要只比较逐维大小——更早到期的钱可以再投资，价值更高。
4. 每年先 `sort` + `unique` 再做支配，避免重复状态互相比较。
5. 一年期利息写成 `yearly + yearly / d`（整数除法即下取整）。

完整代码见 [`P17094.cpp`](P17094.cpp)。

---

## 参考

- [洛谷 P17094](https://www.luogu.com.cn/problem/P17094)
- ICPC 2017 Asia Qingdao Regional — Collecting Cents
