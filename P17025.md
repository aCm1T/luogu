# P17025 [ROI 2026 Day2] 好的染色 题解

## 题意简述

给定一棵以 $1$ 为根、按 DFS 序编号的有根树（保证 $p_i < i$，子树编号连续），在所有叶子上按编号顺序补一条外环，得到平面图。图被划分成若干**面**，相邻面不能同色。

潜能定义为：用不超过 $k$ 种颜色对面进行正确染色的方案数，对 $10^9+7$ 取模。

初始所有树边可见。有 $q$ 次操作，每次翻转一条树边（可见 $\leftrightarrow$ 不可见）。每次操作后重新输出潜能。

**约束**：$\sum n \le 10^6$，$\sum q \le 3\cdot 10^5$，$2 \le k \le 10^9$。

---

## 观察

删边 / 加边只改变面的合并与分裂，对偶图始终是「树状结构 + 外环」诱导出的一类可分解图。面染色计数可以沿着树做**串联组合**（series composition）：每个子树对应一段「从父边到子边」的染色转移，多个兄弟子树按外环顺序串联。

关键不是每次 $O(n)$ 重算，而是用重链剖分把转移矩阵挂在线段树上，单次翻转沿着轻边跳上去更新。

---

## 状态：`State` 与 `Val`

对一段已经组合好的「串联件」，维护：

| 量 | 含义 |
|----|------|
| `a` | 两端颜色强制相同（connected）时的权 |
| `d` | 两端颜色自由时的「开放」转移之一 |
| `e` | 外环闭合时用到的因子 |
| `con` | 这段是否整体仍连通（两端被同一条可见路径拴住） |

`Val` 是压缩后的叶子形式：

- `open = 0`：已闭合 / connected，只用一个数 `x`；
- `open = 1`：开放，保存 `(d, e) = (x, y)`。

`toState` / `staticNode` 在「边可见 / 不可见」与「子树是否 connected」之间做局部折叠。

两个串联件的合并：

$$
\begin{aligned}
a &\leftarrow a_L a_R + (k-1)\, d_L d_R \\
d &\leftarrow a_L d_R + d_L a_R + (k-2)\, d_L d_R
\end{aligned}
$$

`e` 按左右是否 `con` 分四种情况相乘；整体 `con` 当且仅当左右都 `con`。这就是面染色在树边串联时的转移恒等式（两端同色 / 异色对 $k,k-1,k-2$ 的贡献）。

根的答案是：

$$
\text{answer} = k \cdot e(\text{根上所有儿子的串联})
$$

外环需要先选定一个面的颜色（乘 $k$），再乘上整圈闭合因子 `e`。

---

## 转移矩阵：`Trans`

重链上每个点对应一个线性变换，把「子侧重链方向」的状态映到「父侧重链方向」。用三种类型压缩存储（最多 $6$ 个整数）：

| type | 含义 |
|------|------|
| `0` | 恒为 connected，一行 $3$ 个数 |
| `1` | 保持类型，缩放因子 $p$ + $2\times 2$ 块 |
| `2` | 恒为 open，两行各 $3$ 个数（对应 `d` 行 / `e` 行） |

`compose(A, B)` 表示先应用 `B` 再应用 `A`（沿重链从深到浅组合）。`evalBase` 把整条链的合成矩阵作用在单位基上，得到链头的 `Val`。

叶子边：

- 可见：`type=2`，开放单位元；
- 不可见：`type=0`，直接 connected。

内部点：根据「到父亲的边是否可见」以及「轻儿子聚合 `State L` 是否 `con`」，用 `makeInternal` 填出对应矩阵。

---

## 数据结构

题目保证 DFS 序，子树是连续区间，孩子可以用

```text
for (v = u + 1; v < u + sz[u]; v += sz[v])
```

枚举，无需邻接表。

### 1. 重儿子

对每个非根点，在其子树孩子中选 `sz` 最大者为 `heavy`。**根不参与重链**——根的所有儿子都当轻儿子，统一丢进根的聚合树。

### 2. 轻儿子聚合树 `aggTree`

每个点 $u$ 的所有轻儿子（根则是全部儿子）按 DFS 顺序排成叶子，建一棵以 `seriesMerge` 为合并的线段树。查询 `getAgg(u)` 即 $u$ 下方轻边贡献的总 `State`。

### 3. 重链矩阵树 `chainTree`

每条重链（从轻边儿子或根的儿子出发）建线段树，叶子是链上各点的 `Trans`，合并为 `compose`。链头 `dp[h] = evalBase(整条链)`。

---

## 询问更新

翻转边 $v$（`visible[v] ^= 1`）后，从 `cur = v` 向上：

1. 用当前 `visible` 与 `getAgg(cur)` 重算 `nodeMat[cur]`；
2. 写入 `cur` 所在重链线段树，取出新的链头 `newHead`；
3. 若与旧 `dp[h]` 相同则停止（剪枝）；
4. 否则更新 `dp[h]`，并作为轻儿子写入父亲的 `aggTree`；
5. 令 `cur = par[h]`，若已到根则结束。

每次跳过一条轻边，摊还 $O(\log n)$ 段重链；每段线段树修改 $O(\log n)$。单次询问 $O(\log^2 n)$，总复杂度

$$
O\big((n+q)\log^2 n\big)
$$

足以通过 $\sum n,\sum q$ 的上限。

---

## 实现要点

1. **取模**：`K1 = (k-1) mod MOD`，`K2 = (k-2) mod MOD`（注意 $k$ 可达 $10^9$，先模再减）。
2. **单位元**：`identityState = {a=1,d=0,e=1,con=1}`；`transIdentity` 为 type `1` 的单位矩阵。
3. **根特判**：`answer()` 只读 `getAgg(1)`，不再把根放进任何重链。
4. **剪枝**：链头 `Val` 不变则不再向上，实际常数很好。
5. **多测**：每组重建 `sz/heavy/线段树`，注意 `totalAgg/totalChain` 用 `long long` 累加下标。

---

## 复杂度

| 项目 | 复杂度 |
|------|--------|
| 预处理 HLD + 建树 | $O(n\log n)$ |
| 单次翻转 | $O(\log^2 n)$ |
| 总时间 | $O((n+q)\log^2 n)$ |
| 内存 | 聚合树 + 重链树各 $O(n)$ |

---

## 与部分分做法的关系

| 做法 | 适用 | 瓶颈 |
|------|------|------|
| 对偶图删除收缩求色多项式 | $n\le 9$ | 指数 |
| 悬挂删除 + $\chi(\deg')/(k(k-1))$ | $\sum n,\sum q \le 10^4$ | $O(nq)$ |
| 树压缩到 $O(q)$ 再暴力 | $\sum q \le 5\cdot 10^3$ | 压缩细节多 |
| **本文：HLD + 矩阵线段树** | 满分 | 实现矩阵分类 |

悬挂删除公式给出的是「静态一次」的正确计数；满分需要的是同一套串联代数在动态边翻转下的增量维护，本代码直接在转移矩阵层完成。

---

## 参考

- 代码：[`P17025.cpp`](P17025.cpp)
- 题目：https://www.luogu.com.cn/problem/P17025
