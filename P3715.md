# P3715 [BJOI2017] 魔法咒语

## 题意

用 $N$ 个基本词汇拼出长度恰好为 $L$ 的串，要求拼出的串不含任何忌讳子串。不同的拼接方案即使拼出的字符串相同也算不同。答案模 $10^9+7$。

数据特征：前 6 个点 $L\le 100$；后 4 个点基本词汇长度 $\le 2$ 且 $L\le 10^8$。

## 做法

对忌讳词语建 AC 自动机，沿 fail 传递危险标记。

设 $f[i][u]$：拼出长度 $i$、落在 AC 节点 $u$ 的方案数。枚举基本词汇，在自动机上走一遍（中途碰到危险则非法），做转移。

- $L\le 100$：直接 DP。
- 否则（词长只有 1 或 2）：用伴随矩阵把 $(f[i-1], f[i])$ 推到 $(f[i], f[i+1])$，矩阵快速幂算 $f[L]$。

## 复杂度

AC 节点数 $S\le 100$。DP 为 $O(L\cdot S\cdot \sum|w|)$；矩阵为 $O(S^3\log L)$。
